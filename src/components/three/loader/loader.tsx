import { Html } from '@react-three/drei';
import Spinner from '../../ui/spinner/spinner';

const Loader = () => {
  return (
    <Html center>
      <Spinner />
    </Html>
  );
};

export default Loader;
